-- water.hj : cuts to closure; geometry follows from region count
floor 1.0

O := cut 8
H := cut 1

W := close O(H, H)     -- 2:1, four regions, two lone pairs
observe W
assert W.vacancy == 0  emit "water did not close"
